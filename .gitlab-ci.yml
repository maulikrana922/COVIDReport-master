stages:
    - build

build:
    image: reactnativecommunity/react-native-android
    stage: build
    script:
        #- yarn install
        #-  chmod +x gradlew
        #- ./gradlew assembleMyBuild --stacktrace
        - chmod 775 /usr/lib/android-sdk/
        - gradle build
    artifacts:
        paths:
            - app/build/
